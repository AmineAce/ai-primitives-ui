{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "math",
  "title": "Math",
  "description": "clamp() helper for orb math.",
  "files": [
    {
      "path": "packages/orbs/src/lib/math.ts",
      "content": "export function clamp(value: number, min: number, max: number) {\n  return Math.min(Math.max(value, min), max);\n}\n",
      "type": "registry:lib",
      "target": "components/orbs/lib/math.ts"
    }
  ],
  "type": "registry:lib"
}