# AI Primitives

> A dependency-free library of monochrome UI primitives for AI-native interfaces · loading states, thinking traces, streaming text, and more. Motion is drawn with the plain HTML5 Canvas 2D API: no WebGL, no SVG filters, no blur.

## Primitives · 18 listed (17 ready: 16 orbs + StreamingText ship in `@ai-primitives-ui/ui`) + 1 coming soon

- Loading State · 11 (ship): DownloadOrb (Generating File), ErrorOrb (Failed), ProgressOrb (Progress), CloningOrb, SyncOrb, FetchingOrb, PullingOrb, PushingOrb, MergingOrb, RebasingOrb, StashingOrb · Canvas 2D, monochrome, `size` `orbSizes xs 16 → 2xl 96`, `speed`, `paused`, `color`, `className`/`style`, `aria-label` · `ProgressOrb` also accepts `value` 0–1
- Thinking · 5 (ship): DnaOrb (DNA), VerifyOrb (Verify), GraphOrb (Graph), CubeOrb (Thinking), ScanOrb (Scan)
- StreamingText (ships) + MiniOrb/drawGlobe companions (ship, not in registry count)
- More: coming soon

SyncOrb: square 8×8 flat → diagonal shimmer → supernova suck → globe with 3-ring flash (each plane), clipped flash inside sphere, perfect loop 3.6s.

## Install

npm install @ai-primitives-ui/ui
pnpm add @ai-primitives-ui/ui

import { CloningOrb, SyncOrb } from "@ai-primitives-ui/ui";
<CloningOrb size={64} aria-label="Cloning" />
<SyncOrb size={64} aria-label="Sync" />

## Theming

- system via matchMedia + inline THEME_INIT_SCRIPT before hydration (no FOUC)
- --orb-fg on any ancestor, or color prop per instance (alpha via ink())

## Links

- Live: https://ai-primitives-ui.vercel.app
- Docs: https://ai-primitives-ui.vercel.app/docs
- GitHub: https://github.com/AmineAce/ai-primitives-ui
- Playground: https://ai-primitives-ui.vercel.app/#playground · 17 ready live, orbSizes, one-shot Chat test

## License

MIT · Built by AmineAce
